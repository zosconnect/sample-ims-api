---
mappings:
- message:
    mappings:
    - IVTNO_INPUT-MSG:
        mappings:
        - IN-COMMAND:
            template: "ADD"
        - IN-LAST-NAME:
            template: "{{$body.lastName}}"
        - IN-FIRST-NAME:
            template: "{{$body.firstName}}"
        - IN-EXTENSION:
            template: "{{$body.extension}}"
        - IN-ZIP-CODE:
            template: "{{$body.zipCode}}"
